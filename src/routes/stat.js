'use strict';
import React, { Component } from 'react'

export default class Stat extends Component {

    componentDidMount() {
        throw new Error('error');
    }

    render() {
        return (
            <div>stat</div>
        )
    }
}
